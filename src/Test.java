public class Test {
    public static void main(String[] args) {
        // Create an instance of DatabaseConnection
        // DatabaseConnection dbConnection = new DatabaseConnection("jdbc:mysql://localhost:3306/sts", "root", "");
        
        // tests

        // //delete
        // dbConnection.deleteCharacter(4);

        // // Create a Character object with the data you want to insert
        // Character character = new Character("Ironclad", 80, 0);
        // Character character2 = new Character("Silent", 70, 0);
        // // Call the createCharacter() method
        // int generatedId = dbConnection.createCharacter(character);
        // int generatedId2 = dbConnection.createCharacter(character2);

        // // run API
        // API.runAPI();

        // // retreving from API
        // API.retrieveAndPrintCharacters();

        // // swing UI
        // new MenuUI();
        // UI run = new UI();
    }
}